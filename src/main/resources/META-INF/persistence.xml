<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd"
             version="2.1">

    <!-- Persistence Unit tanımı -->
    <persistence-unit name="library_management" transaction-type="RESOURCE_LOCAL">

        <!-- Entity sınıflarının tanımlanması -->
        <class>Author</class>
        <class>Book</class>
        <class>BookBorrowing</class>
        <class>Category</class>
        <class>Publisher</class>

        <properties>
            <!-- 🔗 Veritabanı bağlantısı -->
            <property name="javax.persistence.jdbc.driver" value="org.postgresql.Driver" />
            <property name="javax.persistence.jdbc.url" value="jdbc:postgresql://localhost:5432/library_management" />
            <property name="javax.persistence.jdbc.user" value="postgres" />
            <property name="javax.persistence.jdbc.password" value="uqur5806T" />

            <!-- 🧩 Hibernate ayarları -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQLDialect"/>
            <property name="hibernate.hbm2ddl.auto" value="update"/> <!-- create, create-drop, update, validate -->

            <!-- 🖥️ Konsol çıktısı -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>

            <!-- 📦 JDBC bağlantısında karakter seti sorunu olmaması için -->
            <property name="hibernate.connection.charSet" value="UTF-8"/>

        </properties>
    </persistence-unit>

</persistence>
